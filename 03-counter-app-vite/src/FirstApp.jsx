export const FirstApp = ({ title, subTitle }) => {
  // console.log(props)
  return (
    <>
      <h1>{title}</h1>
      {/*<code>{JSON.stringify(}</code >*/}
      <p>{subTitle}</p>
    </>
  );
};
